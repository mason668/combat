package sim.route;

public class NodeReverse extends Node {
	
	/*TODO
	 * 
	        ELSEIF ( NFLAG .EQ. INODEREVERSE ) THEN
	          CALL SET_REVERSE(IUNIT,ISIDE)
	          CALL SET_UNIT_WINDOW( 2, IUNIT, ISIDE, LUN )
	          CALL ICON_ID( IUNIT, ISIDE, KLINE$ )
	          CALL LINEOUT (1,0,0)
	          WRITE (KLINE$,"('Now reversing')" )
	          CALL LINEOUT (0,0,0)
	          CALL LINEOUT (2,0,0)
	          CALL KDASEL (1)
	          KFLAGNODE(1,IUNIT,ISIDE)    =  INODENORM
	 */

}

package sim.route;

public class NodeCrawl extends Node {
	/*TODO
	        ELSEIF ( NFLAG .EQ. INODECRAWL ) THEN
	          CALL SET_CRAWL(IUNIT,ISIDE)
	          CALL SET_UNIT_WINDOW( 2, IUNIT, ISIDE, LUN )
	          CALL ICON_ID( IUNIT, ISIDE, KLINE$ )
	          CALL LINEOUT (1,0,0)
	          WRITE (KLINE$,"('Now crawling')" )
	          CALL LINEOUT (0,0,0)
	          CALL LINEOUT (2,0,0)
	          CALL KDASEL (1)
	          KFLAGNODE(1,IUNIT,ISIDE)    =  INODENORM
	 * 
	 */

}
